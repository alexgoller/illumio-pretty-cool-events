## Traffic Watcher Alert: {{ traffic_watcher }}

| Field | Value |
|-------|-------|
| PCE | `{{ pce_fqdn }}` |
| Time | {{ timestamp }} |
| Flows Found | **{{ flows_count }}** |
| Decisions | {{ policy_decisions | join(', ') }} |
| Sources | `{{ src_include or 'any' }}` |
| Destinations | `{{ dst_include or 'any' }}` |
| Services | `{{ services_include or 'any' }}` |

{% if sample_flows %}
### Sample Flows

| Source | Destination | Port | Decision | Flows |
|--------|------------|------|----------|-------|
{% for f in sample_flows %}| {{ f.get('Source Hostname', f.get('Source IP', '?')) }} ({{ f.get('Source Application', '?') }}/{{ f.get('Source Environment', '?') }}) | {{ f.get('Destination Hostname', f.get('Destination IP', '?')) }} ({{ f.get('Destination Application', '?') }}/{{ f.get('Destination Environment', '?') }}) | {{ f.get('Port', '?') }}/{{ f.get('Protocol', '?') }} | {{ f.get('Reported Policy Decision', '?') }} | {{ f.get('Num Flows', '?') }} |
{% endfor %}
{% endif %}
